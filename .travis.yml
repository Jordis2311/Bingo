sudo: false
language: python
python:
  - "3.8"
cache: pip
install: 
  - pip install pytest
  - pip install pytest-cov
  - pip install python-coveralls
script:
  - pytest --cov=src
after_success: coveralls
